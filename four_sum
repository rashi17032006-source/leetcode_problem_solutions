class Solution:
    def sumFourDivisors(self,nums):
        total_sum=0
        for i in nums:
            divisors=set()

            for j in range(1,int(i**0.5)+1):
                if i%j==0:
                    divisors.add(j)
                    divisors.add(i//j)          
            if len(divisors)==4:
                total_sum+=sum(divisors)        
        return total_sum    
